-create enviroumnt
   python3 -m venv ./venv

-activate enviroument
   venv/Scripts/activate (for windows)
   . ./ venv/Scripts/activate  (for mac & linux)

-install libraies
   pip install -r requirements.txt

-to run server
   python manage.py runserver

